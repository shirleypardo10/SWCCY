{
  "name": "shir-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "node dev-all.mjs",
    "build": "node build-all.mjs",
    "backend:bootstrap": "node backend-bootstrap.mjs",
    "dev:backend": "npm --prefix yameza-be run start:dev",
    "dev:frontend": "npm --prefix app-web run dev",
    "build:backend": "npm --prefix yameza-be run build",
    "build:frontend": "npm --prefix app-web run build"
  }
}
